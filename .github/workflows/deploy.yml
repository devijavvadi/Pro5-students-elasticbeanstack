name: Deploy PHP App to Terraform Infrastructure

# 1. This "on" block defines what events trigger the pipeline
on:
  push:
    # 2. This restricts the automation to run ONLY when changes land on this specific branch
    branches:
      - main
